import 'package:flutter/material.dart';

class RagnarokColors {
  // Main Color / OnPrimary
  static const Color onPrimary = Color(0xFFFFFFFF);

  // Main Color / Primary Default
  static const Color primaryDefault = Color(0xFF403CE1);

  // Neutral / Shade 1
  static const Color neutralShade1 = Color(0xFF131313);

  // Neutral / Shade 2
  static const Color neutralShade2 = Color(0xFF333333);

  // Neutral / Shade 3
  static const Color neutralShade3 = Color(0xFF5A5A5A);

  // Neutral / Shade 4
  static const Color neutralShade4 = Color(0xFF808080);

  // Neutral / Shade 5
  static const Color neutralShade5 = Color(0xFFA6A6A6);

  // Neutral / Shade 6
  static const Color neutralShade6 = Color(0xFFDADADA);

  // Neutral / Shade 7
  static const Color neutralShade7 = Color(0xFFEBEBEB);

  // Neutral / Shade 8
  static const Color neutralShade8 = Color(0xFFF9F9F9);

  // Neutral / Shade 9
  static const Color neutralShade9 = Color(0xFFFFFFFF);

  // Additional Gray
  static const Color additionalGray = Color(0xFF808080);
}
